package calculating_volume;

public interface Volume {
    double main(int length);
    double main(double radius);
    double main(double radius, double height);
    double main(int length, int base, int height);
}
