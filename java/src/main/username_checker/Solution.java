package username_checker;

/**
 * Find the regex to match username with these criteria
 The username can contain alphanumeric characters and/or underscores(_).
 The username must start with an alphabetic character.
 8 ≤ |Username| ≤ 30.

 Examples
 alpha_naheed
 xahidbuffon
 nagib@007
 123Swakkhar

 Should result in
 Valid
 Valid
 Invalid
 Invalid
 */
public class Solution {
    // Regex pattern that worked
//    String pattern = "^[a-zA-Z]([a-zA-Z|_]+){8,30}";
}
